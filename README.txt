LocSystem : create a system with different device. 

            Inside the class, there is a function called calculate(). It returns a Point object. 
            
Device : there is a function called distance(), it is used to cal the distance between each device and the signal.
